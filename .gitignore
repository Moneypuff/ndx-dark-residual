# Python bytecode
__pycache__/
*.py[cod]

# Local FINRA/Yahoo data cache (rebuilt incrementally; persisted via CI Actions cache)
.ndx_dark_cache/

# Built dashboard pages: generated by the Pages workflow and deployed as a build
# artifact, never committed (only docs/.nojekyll is tracked)
docs/*.html

# Editor / OS cruft
.DS_Store

# Built dashboard pages (deployed as a Pages artifact by CI, never committed)
docs/*.html
