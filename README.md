# claude/optsnap-data

Vol-tracker chain captures. optsnap/ = daily EOD snapshots; intraday/ = hourly
session captures (9:50-15:50 ET). Written by the capture session/workflow; see
VOL_TRACKER.md on the code branch.
